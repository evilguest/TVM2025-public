AddMul {
  Exp = AddExp
  AddExp = AddExp "+" MulExp  -- plus
         | MulExp
  MulExp = MulExp "*" PriExp  -- times
         | PriExp
  PriExp = number
         | "(" Exp ")"  -- paren
  number = digit+
}