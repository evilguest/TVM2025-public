AddMul {
    Exp    = AddExp
    AddExp = AddExp "+" MulExp  -- plus
           | MulExp
    MulExp = MulExp "*" BrExp  -- mul
           | BrExp
    BrExp  = "(" Exp ")"        -- braces
           | number
    number = digit+
}